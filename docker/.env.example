# MTProxy Configuration
# Copy this file to .env and set your values

# Secret key for client connections
# Generate with: head -c 16 /dev/urandom | xxd -ps
MTPROXY_SECRET=CHANGE_ME_SECRET

# Optional: Proxy tag from @MTProxybot
# Register your proxy at https://t.me/MTProxybot
MTPROXY_TAG=

# Number of worker processes
# Increase for high load (recommended: number of CPU cores)
WORKERS=1

# Port configuration (if you need to change defaults)
# MTPROXY_PORT=443
# STATS_PORT=8888
